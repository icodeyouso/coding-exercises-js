var removeVowels_1 = function(text) {
        var result = "";
        for (var i = 0; i < text.length; i++) {
            var c = text[i];
            if ("aeiou".match(c.tolowerCase()) == null {
                    result += c
                }
            }
            return result

            var removeVowels_2 = function(text) {
                    var result = ""
                    result = text.where(c => !"aeiou".any(x => x == c.toLowerCase()));
                    return result;
                    string.prototype.where = function(predictable) {
                            string.prototype.any = function(predicatable) {

                            }
                            var removeVowels_3 = function(text) {
                                run();
                            }