function stringMeAlong(str) { //function to find substring, argument is a string
    var myString = str
    var count = 0 // zero will be the starting point when counting for substring
    var returnStr = "" //set to empty string to hold position for returned substring
    for (var i = 0; i = count;) {
        count = tempCount
        returnStr = tempReturnStr.join("") // join the collected substrings to for the final output

    }

}
return returnStr;